# AGENTS.md

## Project Documents

- 项目规则以 `rules/README.md` 为准，开始修改前必须阅读。
- 整体架构以 `impl_docs/architecture/overview.md` 为准，不得引入违反依赖方向的实现。
- 待实施工作维护在 `impl_docs/TODO.md` 和 `impl_docs/plans/`。
- 完成修改后，必须同步更新任务状态，并在 `impl_docs/changes/` 添加变更记录。
- 若代码行为与文档不一致，应在同一次修改中更新相关文档。

## Communication

- 默认使用简洁中文沟通。
- 代码、命令、路径、标识符、API 名称和错误信息保留英文。
- 直接给出结论和可执行内容，避免冗长背景说明。
- 需求存在歧义、缺少关键约束或可能产生明显不同实现时，先提问确认，不要自行猜测。
- 提问应具体、简短，并说明需要确认的决策点。

## Development Principles

- 优先实现满足当前需求的最小可用方案。
- 保持代码简洁、清晰、可维护，避免过度包装和过度设计。
- 遵循现有项目结构、命名和代码风格。
- 修改范围应尽量小，不顺带重构无关代码。
- 不提前设计尚未需要的扩展能力。
- 优先使用项目已有依赖和工具；引入新依赖前说明必要性。

## Code Style

- 使用直接、易读的实现，避免不必要的抽象和间接层。
- 一次性逻辑直接放在调用处，不为形式统一拆分大量小函数。
- 仅在逻辑复用、职责独立或明显提升可读性与可测试性时提取函数。
- 除非确有需要，不新增 wrapper、factory、registry、manager、base class 或复杂配置层。
- 命名应准确表达用途，避免含糊缩写。
- 注释只解释不明显的原因、约束或边界条件，不复述代码行为。
- 不保留未使用代码、占位抽象或无明确用途的配置项。

## Workflow

1. 修改前先阅读相关代码、配置和测试，确认现有行为。
2. 若验收标准、输入输出、技术边界或兼容要求不明确，先向用户确认。
3. 实施最小且针对性的修改。
4. 运行与改动直接相关的测试、格式检查或静态检查。
5. 汇报修改内容、验证结果，以及仍存在的限制或未确认事项。

## Safety

- 不覆盖或回退用户已有但与当前任务无关的修改。
- 不执行破坏性操作，除非用户明确授权。
- 不暴露密钥、凭据、隐私数据或本地敏感信息。
- 涉及数据删除、接口破坏性变更、生产环境操作或高成本任务时，执行前必须确认。
