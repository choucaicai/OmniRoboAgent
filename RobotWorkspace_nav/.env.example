# Robot-side URL. Point this to the GPU server or an SSH-local-forward endpoint.
LATENTPILOT_SERVER_URL=http://127.0.0.1:5801/eval_vln

# Server-side optional defaults.
# LATENTPILOT_MODEL_PATH=/models/latentpilot
# LATENTPILOT_OUTPUT_DIR=/data/latentpilot-runs
